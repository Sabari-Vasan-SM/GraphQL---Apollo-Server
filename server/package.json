{
  "name": "graphql-apollo-server",
  "version": "2.0.0",
  "description": "Enhanced GraphQL Apollo Server with data persistence, logging, and advanced features",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "logs": "tail -f logs/$(date +%Y-%m-%d).log"
  },
  "keywords": ["graphql", "apollo-server", "nodejs", "api"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "apollo-server": "^3.13.0",
    "graphql": "^16.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "eslint": "^8.50.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
